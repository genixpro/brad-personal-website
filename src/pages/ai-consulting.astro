---
import { getEntry } from 'astro:content';
import ServiceRoutePage from '../components/services/ServiceRoutePage.astro';

const service = await getEntry('services', 'ai-consulting');
if (!service) throw new Error('Missing service content: ai-consulting');
---

<ServiceRoutePage service={service} className="subpage-ai-consulting" />
