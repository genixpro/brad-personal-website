---
import { getEntry } from 'astro:content';
import ServiceRoutePage from '../components/services/ServiceRoutePage.astro';

const service = await getEntry('services', 'code-review-services');
if (!service) throw new Error('Missing service content: code-review-services');
---

<ServiceRoutePage service={service} className="subpage-code-review-services" />
